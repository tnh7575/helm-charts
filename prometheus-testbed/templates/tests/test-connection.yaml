apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "prometheus-testbed.fullname" . }}-test-connection"
  labels:
    {{- include "prometheus-testbed.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "prometheus-testbed.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
