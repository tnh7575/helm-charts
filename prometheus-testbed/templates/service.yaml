apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-testbed.fullname" . }}
  labels:
    {{- include "prometheus-testbed.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "prometheus-testbed.selectorLabels" . | nindent 4 }}
